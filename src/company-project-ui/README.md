# Meter Readings UI (Angular)

Angular UI for the Meter Readings API.

## Setup

```bash
npm install
npm start
```

Make sure your .NET API is running at the URL configured in `src/environments/environment.ts`.
